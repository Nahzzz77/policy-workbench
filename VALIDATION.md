# 首版验收记录

2026-09-05，北京时间。

## 已验证
- 8 个用户指定官网均有真实内容入库。
- 本地 SQLite 与发布构建中的初始快照共 482 篇，432 篇有提取正文，50 篇仅有索引；482 篇均识别到官网发布日期。
- 家政正文及标题检索命中 18 条。
- 6 项可运行测试通过，包含 URL 安全边界、robots 路径规则、GB 编码、朝阳文章地址、正文区域、日期、去重保留首次时间、正文多词检索、来源增停、失败记录及 CSRF 校验。
- 打包 Worker 以独立临时数据库执行：首页、脚本、初始化、状态、家政查询返回 200；初始简报已存在。
- 首份简报由真实 Codex CLI 生成，1114 字符，输出经过结构及来源 URL 检查。
- 每日 Codex 心跳任务已创建，Asia/Shanghai 07:00，ACTIVE，来源从线上数据库动态读取。
- Sites 私有版本 1 发布成功。未登录访问返回登录页/401，与私有访问设计一致。

## 尚待用户首次登录后验证
- 线上浏览器初始化 D1 后的数据读取和真实同步端到端检查。
- 次日定时任务端到端运行；电脑需可运行、联网，浏览器需有效登录。
- 浏览器提供 WebMCP 注册接口，但原生工具执行 API 与常见接口不同；搜索工具已注册，完整 WebMCP 调用验证尚未完成。常规搜索 API 已验证。

## 已知覆盖限制
- 抓取当前入口与发现的有限政策/通知栏目，每来源每轮 24 篇；不是全站历史全量库。
- 不抓 robots 禁止路径，不处理验证码、登录内容；404 和错误被记录。
- 图片、视频、扫描附件暂不 OCR，仅按官方标题索引。部分官网 DOM 改版仍需要适配。
- URL 去重保留出处，不同官网转载不做语义合并。
