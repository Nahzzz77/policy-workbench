# 数据保留与公司工作区规则

## 默认建议

- 政策文章正文：保留 12 个月。
- 可选策略：6、12、24、36 个月。
- 收藏、已关联政策、待办、企业资料和人工备注：不因自动清理删除。
- 超过期限的普通文章：先删除正文和采集明细，保留标题、来源、发布日期、原文链接和摘要索引。
- 运行日志：保留 12 个月。

清理任务必须使用 UTC 时间计算并在界面显示北京时间的预计范围。执行前统计将删除的文章、正文字节数、运行记录和附件链接数量；执行后保存清理批次、策略和结果。失败任务可重试，不能把失败显示成完成。

## 换公司

当前账号对应多个 company workspace，当前只有一个 active workspace。新增公司工作区时复制来源配置，不复制企业资料、匹配、待办或原始附件。

“清空当前公司并开始新公司”删除当前 workspace 的 company facts、qualifications、matches、tasks、reminders、notes 和关联关系；全局 sources、articles、runs、digests 保留。政策文章与公司解绑后按普通保留策略处理。

操作必须要求用户输入确认文字，清空前可导出 JSON/CSV，创建 30 天恢复包；只在恢复期内保留敏感导出包，过期自动销毁。任何 UI、API 或定时任务都必须带 workspace_id，不能用全局查询返回另一公司资料。

## 暂不自动做的事情

不会因为“换公司”自动删除全部政策历史；不会把同一个登录账号当成不同企业身份；不会无确认地清理企业税收、资质或待办资料。
