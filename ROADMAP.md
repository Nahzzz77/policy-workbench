# 第二阶段实现拆分

1. 数据层：companies、company_facts、policies、policy_conditions、policy_matches、tasks、task_reminders。企业字段和政策条件带更新时间及来源依据。
2. 抽取层：从归档正文生成条件草稿；保留原文片段、段落、置信度和人工确认状态。未确认条件不可标为“符合”。
3. 匹配层：确定性规则先判断数值、日期、地区和资质；AI 只处理自然语言解释与缺失字段建议。企业资料变化后可重算。
4. 待办层：关联 company_id 和 policy_id，支持计划日、截止日、提醒偏移、状态、忽略与软删除。同一任务同一偏移只通知一次。
5. 工作台：企业切换器、匹配结果、待办窗口、未来 3 个月、逾期、政策依据和企业资料编辑页。
6. 交付：先做本地优先压缩包，隔离每个客户的数据目录；在线多租户作为独立版本。

暂不自动提交申报，不替代政府审核，不上传原始税务/资质材料给第三方，不把关键词命中当成具备申请资格。
